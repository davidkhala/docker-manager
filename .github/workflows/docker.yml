name: docker test

on:
  push:

jobs:

  nodejs:
    defaults:
      run:
        working-directory: nodejs
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: systemctl start --user docker
    - run: npm i
    - run: npm test
